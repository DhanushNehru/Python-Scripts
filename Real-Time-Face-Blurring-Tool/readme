# Face Blurring Application

A Python script that detects and blurs faces in images, videos, and webcam feeds using OpenCV and deep learning.

## Features
- Face detection using DNN-based model
- Real-time face blurring for webcam feed
- Video processing with face anonymization
- Adjustable blur strength and confidence thresholds

## Requirements
- Python 3.6+
- OpenCV (`opencv-python`)
- NumPy
- Model files:
  - `deploy.prototxt.txt` in `./protocol/`
  - `res10_300x300_ssd_iter_140000_fp16.caffemodel` in `./model/`

## Installation
```bash
pip install opencv-python numpy